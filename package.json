{
  "name": "truthshield-ai",
  "version": "1.0.0",
  "description": "TruthShield AI - Agentic Scam Investigation Platform Root",
  "private": true,
  "type": "module",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix mcp && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently --kill-others npm:dev:backend npm:dev:frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
